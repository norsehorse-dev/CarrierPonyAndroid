// Color.kt
// CarrierPony Android
//
// The icon palette, exact hex from CarrierPony-AppIcon-1024 — the same values
// iOS CPTheme samples, so the two apps read as one product.

package com.carrierpony.app.ui.theme

import androidx.compose.ui.graphics.Color

val CPGold = Color(0xFFEBC289)        // gradient top-left
val CPCoral = Color(0xFFF28B80)       // gradient midpoint
val CPRed = Color(0xFFF1667B)         // gradient bottom-right

val CPAccent = Color(0xFFEC6755)      // horse mid — sent bubbles, primary
val CPAccentLite = Color(0xFFEF8055)  // horse upper (coral)
val CPAccentDeep = Color(0xFFF15056)  // horse lower (red)
